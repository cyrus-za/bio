{
  "name": "begin-app",
  "version": "0.0.0",
  "description": "Basic Begin personal website",
  "scripts": {
    "start": "cross-env NODE_ENV=testing npx sandbox",
    "lint:fix": "prettier --check '*/**/*.{js,ts,tsx,jsx,graphql,md,html}'"
  },
  "devDependencies": {
    "@architect/sandbox": "latest",
    "cross-env": "^7.0.0",
    "prettier": "^1.19.1"
  }
}
